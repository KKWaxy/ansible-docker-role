---
- name: "Remove docker installation packages on Ubuntu"
  become: True
  apt:
    name: "{{ item }}"
    state: absent
  loop: 
    - docker
    - docker-engine
    - docker.io 
    - containerd
    - runc
  when: ansible_distribution == 'Ubuntu'

- name:  Delete docker files on Ubuntu
  become: True
  file:
   path: /var/lib/docker
   state: absent
   mode: u+wr,g+wr,o+wr 
  when: ansible_distribution == 'Ubuntu'
  
- name:  Delete containerd files on Ubuntu
  become: true
  file:
    path: /var/lib/containerd
    state: absent
    mode: u+wr,g+wr,o+wr
  when: ansible_distribution == 'Ubuntu'
...